#include "main.h"

/**
 * _strlen - Returns the length of a string
 * @s: A pointer to the character string to measure
 *
 * Return: The total number of characters in the string
 */
int _strlen(char *s)
{
	int length;

	for (length = 0; s[length] != '\0'; length++)
	{
		/* The loop body is empty because the math happens in the header */
	}

	return (length);
}
